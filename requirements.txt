# ============================================================================
# Vehicle Proximity Detection System - Dependencies
# ============================================================================
#
# CRITICAL: PyTorch with CUDA support MUST be installed FIRST, separately.
# Do NOT install from this file directly — it will pull CPU-only PyTorch.
#
# STEP 1 — Install PyTorch with CUDA 12.8 (for RTX 5050):
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu128
#
# STEP 2 — Then install the rest:
#   pip install -r requirements.txt
#
# ============================================================================

# YOLOv11 / YOLOv10 object detection + ByteTrack integration
ultralytics>=8.3.0

# OpenCV for video I/O and frame rendering
opencv-python>=4.10.0

# NumPy (pinned to avoid ABI breaks with OpenCV/PyTorch)
numpy>=1.26.0

# tqdm for progress bar during frame iteration
tqdm>=4.66.0

# lap — Linear Assignment Problem solver required by ByteTrack
lap>=0.5.0

# imageio-ffmpeg — ships a static ffmpeg binary for H.264 video conversion
# Required so the output video plays in Gradio / browsers
imageio-ffmpeg>=0.5.0
